# 14th exercise. need to use the code from find largest number exercise, make a function, store it under utils.py module, then call it on this python file.

from utils import find_largest

numbers = [1, 3, 2, 7, 5, 9, 1, 1, 2, 4, 8]
print(find_largest(numbers))
